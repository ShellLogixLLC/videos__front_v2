export type RoutesProps = {
  routeName: string;
  pageName: string;
  id: number;
};

export type FilteredProp = {
  nameRoute: string;
  id: number;
};

export type LangData = {
  name: string;
  locale: string;
};

export type Comment = {
  id: number;
  name: string;
  comment: string;
};
